import React from 'react';

const AppDetails = () => {
    // const { image, title, companyName, id, description, size, reviews, ratingAvg, downloads, ratings } = AllApps
    return (
        <div>
            
        </div>
    );
};

export default AppDetails;